apiVersion: v1
kind: Service
metadata:
  name: spring-boot-service
spec:
  selector:
    app: spring-boot-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
  type: ClusterIP  # Utilise LoadBalancer si tu veux l'exposer en externe
